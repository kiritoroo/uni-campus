import { SoundFxProvider } from "../contexts/SoundFxContext";

const CampusScreen = () => {
  return (
    <SoundFxProvider>
      <div>campus scene</div>
    </SoundFxProvider>
  );
};

export default CampusScreen;
