export type TSoundEffectData = {
  name: string;
  volume: number;
  file_url: string;
};

export type TCambusBuildingData = {
  name: string;
  model_url: string;
  label: string;
  uses: string;
};
